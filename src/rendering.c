#include "rendering.h"

void render(Player *player, Map *map) {
    // Clear the screen
    // Call raycasting to draw the scene
    // Render the player and any other game objects
    SDL_RenderPresent(renderer);
}